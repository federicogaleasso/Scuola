/*
Galeasso Federico 3C 8/3/2022 Vettori CHAR

Scrivere un programma che permetta di gestire i seguenti vettori:

- InizialiAlunni (contiene la lettera iniziale dell'alunno - char)
- Informatica (contiene i voti di informatica di ogni alunno)
- Sistemi (contiene i voti di sistemi di ogni alunno)
- Italiano (contiene i voti di italiano di ogni alunno)
- Matematica (contiene i voti di matematica di ogni alunno)

Il numero di alunni deve essere chiesto in input e deve essere compreso tra 6 e 26
La lettera iniziale degli alunni è chiesta in input all'utente e deve essere minuscola
I voti di ogni materia devono essere creati random tra 2 e 10
Il nome di ogni studente è la sua iniziale sommata al numero di posizione nel vettore inizialiAlunni (es. 'C' in posizione 3 diventa in stampa "C3", 'G' in posizione 4 diventa in stampa "G4",...)

Stampare:
tutti i vettori

Calcolare:
- La media dei voti di ogni alunno indipendentemente dalla materia
- Calcolare e stampare le iniziali degli alunni che hanno la stessa media
- Media dei voti della classe per ogni materia
*/

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

//PROTOTIPI


int main(){

return 0;
}
